version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: feeder-service
    environment:
      - DB_USER=user
      - DB_PASS=pass
      - DB_HOST=192.168.100.77
      - DB_PORT=3307
      - DB_NAME=feeder-service
    ports:
      - "9000:9000"
      - "9001:9001"
