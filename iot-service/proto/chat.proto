syntax = "proto3";
package chat;

option go_package = "./proto;chat";

message Message {
  string title = 1;
  string content = 2;
}

message MessageTitle {
    string title = 1;
  }

service ChatService {
  rpc SayHello(MessageTitle) returns (Message) {}
}
